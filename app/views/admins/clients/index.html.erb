<div class="container-fluid">
  <a href="/admins/clients/new">
    <button type="button" class="btn btn-success" style="margin-left:15px ;"><i class="fas fa-user-plus"></i>
    <b>Novo Cliente</b></button>
  </a>
<div class="card-body">
  <div class="table-responsive">
    <table id="myTable" class="table table-bordered table-striped table-hover" width="100%" cellspacing="0">
      <thead>
        <tr>
          <th>ID</th>
          <th>Nome Completo</th>
          <th>Celular</th>
          <th>RG</th>
          <th>Criança</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @clients.each do |var| %>
          <tr>
            <td> <%= var.id %> </td>
            <td> <%= var.name_full %> </td>
            <td> <%= var.rg %> </td>
            <td> <%= var.cellphone %> </td>
            <td> <%= humanize_boolean(var.child) %> </td>
            <td>
              <%= link_to edit_admins_client_path(var),
                class: "btn btn-primary btn-xs btn-green-alt" do %>
                <i class="fas fa-edit"></i>
                Editar
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
</div>
